version: '3.8'

services:
  armbench:
    build:
      context: ..
      dockerfile: docker/Dockerfile.arm64
    platform: linux/arm64
    ports:
      - "8000:8000"
    volumes:
      - ../models:/app/models
    environment:
      - PORT=8000
    restart: unless-stopped
